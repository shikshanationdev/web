import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "plus.unsplash.com",
      "images.unsplash.com",
      "files.wiseapp.live",
      "cdn.wiseapp.live",
      "shikshanation.com",
    ]
  }
};

export default nextConfig;
